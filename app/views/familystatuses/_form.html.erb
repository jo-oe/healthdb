<%= form_for(familystatus) do |f| %>
  <% if familystatus.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(familystatus.errors.count, "error") %> prohibited this familystatus from being saved:</h2>

      <ul>
      <% familystatus.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %>
    <%= f.text_field :code %>
  </div>

  <div class="field">
    <%= f.label :text %>
    <%= f.text_field :text %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
